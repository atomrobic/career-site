{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cp -r backend/myproject/* /app/"
  },
  "deploy": {
    "startCommand": "gunicorn myproject.wsgi:application",
    "restartPolicyType": "ON_FAILURE"
  }
}